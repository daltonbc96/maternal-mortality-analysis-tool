library(shiny)
library(plotly)
library(dplyr)


# Preciso criar uma função que vai filtrar dados por pais
# criar modulo que recebe dado filtrado 




server <- function(input, output, session) {


  
}
